node_modules
.next
.env*.local
.DS_Store

# vendored OCR engine — regenerate with `npm run vendor:ocr`
public/tesseract

# Static export for the Android build — regenerate with `npm run android:build`
/out
